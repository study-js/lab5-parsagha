var users = document.getElementById('users');
var empty = document.getElementById('empty-list');

alert("users empty: " + (users.children.length == 0));
alert("empty-list empty: " + (empty.children.length == 0));